// Fixed portfolio/services theme shared by every public-facing page —
// deliberately NOT the tenant's dynamic brand color (theme.accent/accentSoft
// from useTenantBranding). A fixed light theme + purple/teal palette applies
// regardless of tenant branding, keeping every public page visually consistent.
export const ACCENT = '#4B116B'
export const ACCENT_SOFT = '#F0E4F5'
export const TEAL = '#0F766E'
export const TEAL_SOFT = '#CCFBF1'
export const BTN_SHADOW = '0 10px 25px -8px rgba(75, 17, 107, 0.5)'
